#!/bin/bash
# .bashrc can't be moved, it's path is hardcoded

shopt -s extglob

# TODO: Deal with the rest of the bash files, some of them might be set
# to a different location
# ~/.bashrc
# ~/.bash_history
# ~/.bash_profile
# ~/.bash_login
# ~/.bash_logout
