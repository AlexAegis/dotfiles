xdg
# for fallback setup `bash`, `curl` and `unzip` is needed
bash
curl
zip
chocolatey ? [ $windows ]
# Even though it's not directly used, the install script of fnm does use it
# when available
brew ? [ $mac ]
