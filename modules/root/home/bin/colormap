#!/bin/zsh
# Prints a colormap
# Taken from Powerline10k's autogenerated config file
# TODO: Cool-retro-term only correctly prints the first line
for i in {0..255}; do print -Pn "%K{$i} %k%F{$i}${(l:3::0:)i}%f " ${${(M)$((i%8)):#7}:+$'\n'}; done
