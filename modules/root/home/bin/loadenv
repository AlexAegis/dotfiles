#!/bin/sh
# Loads every `*.env` file in the home and in the .env folder

echo Loading Environment $0 $1
set -a
. $HOME/bin/sourcein "$HOME/" ".*\.env"
. $HOME/bin/sourcein "$HOME/.env/" ".*\.env"
set +a
