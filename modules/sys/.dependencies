xdg
coreutils
# TODO: once conditionfiles are done, this can be replaced with a tag
# and the module itself with sys-base or something
sys-arch ? [ $arch ]
sys-debian ? [ $debian ]
sys-gentoo ? [ $gentoo ]
sys-mac ? [ $mac ]
sys-ubuntu ? [ $ubuntu ]
sys-void ? [ $void ]
